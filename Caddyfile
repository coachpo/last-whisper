:8008 {
    # Backend API - proxy /apis/* requests to backend (must come first)
    handle /apis/* {
        uri strip_prefix /apis
        reverse_proxy backend:8000
    }
    
    # Health check endpoint - also proxy to backend
    handle /health {
        reverse_proxy backend:8000
    }
    
    # Frontend - serve static files and handle client-side routing (catch-all)
    handle /* {
        reverse_proxy frontend:3000
    }
    
    # Enable compression
    encode gzip
    
    # Security headers (HTTP version)
    header {
        # Prevent clickjacking
        X-Frame-Options "SAMEORIGIN"
        # XSS protection
        X-Content-Type-Options "nosniff"
        # Referrer policy
        Referrer-Policy "strict-origin-when-cross-origin"
    }
    
    # Logging
    log {
        output file /var/log/caddy/access.log
        format json
    }
}
