# Deployment configuration

# Optional: how long deploy.sh waits for services to become healthy (seconds)
# MAX_WAIT_SECONDS=60

# Optional: credentials for pulling private GHCR images. Leave commented if images are public.
# GITHUB_USERNAME=coachpo
# GITHUB_TOKEN=ghp_your_token_here
